[project]
name = "slt-grokking"
version = "0.1.0"
description = "LLC as a signature of phase transitions in transformers (SLT × Grokking)"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.1.0",
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
    "torchvision>=0.16.0",
]

[project.scripts]
train = "train:main"

[tool.uv]
dev-dependencies = []
